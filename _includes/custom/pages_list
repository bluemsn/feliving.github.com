{% for weight in (1..10) %}
  {% for node in pages_list %}
    {% if node.title != null %}
    <li class="active"><a href="{{ BASE_PATH }}{{node.url}}" class="active">{{node.title}}</a></li>

    {% endif %}
  {% endfor %}
{% endfor %}